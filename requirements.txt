torch
transformers==4.36.0
peft==0.7.0
trl==0.7.4
accelerate==0.25.0
datasets==2.16.0
scikit-learn==1.3.2
matplotlib==3.8.2
# Optional: install manually only if your CUDA/PyTorch setup supports building it.
# The training code falls back to standard attention when flash-attn is unavailable.
# flash-attn==2.4.2
modelscope
